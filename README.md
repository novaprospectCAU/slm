# SLM (Scratch Language Model)

이 저장소는 NumPy + 직접 구현한 autograd로 언어모델을 바닥부터 만드는 학습 프로젝트입니다.

입문자용 상세 매뉴얼:

- `docs/manual_ko.md`

빠른 실행:

```bash
python -m pip install --upgrade pip
pip install -e ".[dev]"
python -m pytest -q
python demos/demo_mlp.py
```

